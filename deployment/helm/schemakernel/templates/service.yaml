apiVersion: v1
kind: Service
metadata:
  name: {{ include "schemakernel.fullname" . }}
  labels:
    {{- include "schemakernel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "schemakernel.selectorLabels" . | nindent 4 }}
