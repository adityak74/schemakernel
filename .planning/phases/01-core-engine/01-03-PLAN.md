---
phase: "01-core-engine"
plan: "03"
type: "execute"
wave: 1
depends_on: []
files_modified: ["tests/test_planner.py", "tests/conftest.py"]
autonomous: true
requirements: ["REQ-02", "REQ-03", "REQ-04"]
must_haves:
  truths:
    - "PlannerClient correctly handles OpenAI provider path"
    - "PlannerClient correctly handles and re-raises InstructorRetryException as PlannerRetryExhausted"
    - "System achieves 100% test coverage across all core engine modules"
    - "End-to-end flow from session start to multi-turn completion is verified with mocked LLM"
  artifacts:
    - path: "tests/test_planner.py"
      provides: "Full coverage for PlannerClient logic"
  key_links:
    - from: "tests/test_planner.py"
      to: "schemakernel/planner.py"
      via: "pytest"
---

<objective>
Verify Instructor-based planner integration and achieve 100% total coverage.
This plan targets the remaining gaps in the PlannerClient and performs a final integration verification of the Phase 1 core engine.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/TESTING.md
@schemakernel/planner.py
@tests/test_planner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Planner Client Tests</name>
  <files>tests/test_planner.py</files>
  <action>
    Add tests to `test_planner.py` covering:
    - `provider="openai"` path in `_build_client` and `call`. Mock `openai.OpenAI` and `instructor.from_openai`.
    - `InstructorRetryException` handling: Mock the instructor client to raise an exception with this name, and assert `PlannerRetryExhausted` is raised.
    - Unsupported provider exception.
  </action>
  <verify>
    <automated>pytest tests/test_planner.py -v --cov=schemakernel/planner.py --cov-report=term-missing</automated>
  </verify>
  <done>Planner client paths are 100% covered</done>
</task>

<task type="auto">
  <name>Task 2: Final Integration Verification</name>
  <files>tests/test_workflow.py</files>
  <action>
    Add a complex multi-turn integration test in `tests/test_workflow.py` (or a new `tests/test_integration.py` if preferred) that:
    1. Starts a session with baseline questions.
    2. Submits answers.
    3. Runs a planner turn that adds new fields.
    4. Submits answers for new fields.
    5. Runs a planner turn that completes the session.
    6. Verifies the `CompletionOutcome` and audit logs.
  </action>
  <verify>
    <automated>pytest tests/ -v --cov=schemakernel --cov-report=term-missing</automated>
  </verify>
  <done>Phase 1 core engine is verified end-to-end with 100% coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SDK -> LLM Provider | Communication with external providers must handle failures gracefully |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Availability | PlannerClient | mitigate | handle LLM retry exhaustion and provider errors gracefully |
</threat_model>

<success_criteria>
Total project coverage (excluding tests) is 100% or as close as practically possible (>= 99%).
All Phase 1 requirements (REQ-01 to REQ-04) are verified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-03-SUMMARY.md`
</output>
