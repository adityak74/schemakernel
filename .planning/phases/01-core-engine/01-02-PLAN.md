---
phase: "01-core-engine"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified: ["tests/test_workflow.py"]
autonomous: true
requirements: ["REQ-04"]
must_haves:
  truths:
    - "Workflow state machine correctly applies all ActionTypes (REMOVE, REQUIRE, HIDE, SHOW, REORDER, COMPLETE, ESCALATE)"
    - "Workflow state machine correctly evaluates all ConditionOperators (GT, GTE, LT, LTE, IN, NOT_IN, etc.)"
    - "Workflow state machine enforces turn limits and prevents execution beyond max_turns"
    - "Workflow stage transitions (INITIALIZED -> COLLECTING -> COMPLETE/ESCALATED) are verified"
  artifacts:
    - path: "tests/test_workflow.py"
      provides: "Full coverage for workflow state machine logic"
  key_links:
    - from: "tests/test_workflow.py"
      to: "schemakernel/workflow.py"
      via: "pytest"
---

<objective>
Reach 100% coverage for the Workflow State Machine.
This plan targets the remaining gaps in action application, conditional logic evaluation, and state transition edge cases.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/TESTING.md
@schemakernel/workflow.py
@tests/test_workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Action Application Tests</name>
  <files>tests/test_workflow.py</files>
  <action>
    Add tests to verify all `ActionType` implementations in `_apply_actions`:
    - `REMOVE`: Ensure field is removed from `fields`, `field_order`, and `answers`.
    - `REQUIRE`: Ensure `required` flag is set to True.
    - `HIDE`: Ensure `visible_if` is set to `ALWAYS_HIDDEN`.
    - `SHOW`: Ensure `visible_if` is set to None.
    - `REORDER`: Ensure field is moved to correct position in `field_order`.
    - `COMPLETE` & `ESCALATE`: Ensure `stage` is updated and `CompletionOutcome` is saved.
    - Failure Case: `submit_answer` with `raise_on_failure=True` for non-existent field.
  </action>
  <verify>
    <automated>pytest tests/test_workflow.py -v --cov=schemakernel/workflow.py --cov-report=term-missing</automated>
  </verify>
  <done>Workflow action application paths are 100% covered</done>
</task>

<task type="auto">
  <name>Task 2: Complete Conditional Logic & Operator Tests</name>
  <files>tests/test_workflow.py</files>
  <action>
    Add tests to verify all `ConditionOperator` implementations in `_eval_single`:
    - `IS_EMPTY`, `NOT_EMPTY`
    - `EQ`, `NEQ`
    - `GT`, `GTE`, `LT`, `LTE` (including non-numeric failure cases)
    - `IN`, `NOT_IN`
    - Verify `get_next_fields` correctly filters hidden fields and already answered fields.
  </action>
  <verify>
    <automated>pytest tests/test_workflow.py -v --cov=schemakernel/workflow.py --cov-report=term-missing</automated>
  </verify>
  <done>Conditional logic evaluation paths are 100% covered</done>
</task>

<task type="auto">
  <name>Task 3: Turn Limits and Prompt Construction Tests</name>
  <files>tests/test_workflow.py</files>
  <action>
    Add tests for:
    - `TurnLimitExceeded` exception when `turn_count >= max_turns`.
    - `_build_system_prompt` coverage for `glossary`, `prohibited_topics`, `exception_rules`, and `completion_criteria`.
  </action>
  <verify>
    <automated>pytest tests/test_workflow.py -v --cov=schemakernel/workflow.py --cov-report=term-missing</automated>
  </verify>
  <done>Lifecycle and prompt construction paths are 100% covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Workflow -> Storage | State must be persisted correctly to prevent corruption |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | Turn Limit | mitigate | enforce max_turns to prevent infinite LLM loops |
| T-01-04 | Tampering | State Transitions | mitigate | validate transition types and current stage before applying actions |
</threat_model>

<success_criteria>
`schemakernel/workflow.py` reaches 100% coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-02-SUMMARY.md`
</output>
