---
phase: "01-core-engine"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["tests/test_validation.py", "tests/test_models.py"]
autonomous: true
requirements: ["REQ-01", "REQ-03"]
must_haves:
  truths:
    - "Validation engine rejects unknown field/action/validator types in planner response"
    - "Validation engine detects and rejects unsafe patterns in ui_props"
    - "Validation engine enforces prohibited topics across all planner output fields"
    - "All validator types (POSITIVE, NON_NEGATIVE, DATE_BEFORE, etc.) are verified"
  artifacts:
    - path: "tests/test_validation.py"
      provides: "Full coverage for validation logic"
    - path: "tests/test_models.py"
      provides: "Validation for duplicate baseline keys in PolicyConfig"
  key_links:
    - from: "tests/test_validation.py"
      to: "schemakernel/validation.py"
      via: "pytest"
---

<objective>
Reach 100% coverage for the Validation Engine and Policy Model.
This plan targets the remaining gaps in validation logic, specifically error cases, safety checks, and edge cases in type coercion and rule evaluation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/TESTING.md
@schemakernel/validation.py
@schemakernel/policy.py
@tests/test_validation.py
@tests/test_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Planner Response Validation Tests</name>
  <files>tests/test_validation.py</files>
  <action>
    Add tests to `TestPlannerResponseValidation` covering:
    - Unknown action type (manipulate `ActionType` or use a mock)
    - Unknown field type
    - Unknown validator type
    - Safety: `ui_props` containing forbidden patterns (e.g., "__", "eval", "<script")
    - Prohibited topics in `FieldDefinition` (key, label, description)
    - Prohibited topics in `next_prompt_context`
  </action>
  <verify>
    <automated>pytest tests/test_validation.py -v --cov=schemakernel/validation.py --cov-report=term-missing</automated>
  </verify>
  <done>Validation engine error paths for planner response are 100% covered</done>
</task>

<task type="auto">
  <name>Task 2: Complete Answer Validation & Coercion Tests</name>
  <files>tests/test_validation.py</files>
  <action>
    Add tests to `TestAnswerValidation` covering:
    - Coercion: NUMBER, BOOLEAN (yes/no/1/0/0.0), DATE (from dateutil), DATETIME (from dateutil)
    - ValidatorType: POSITIVE, NON_NEGATIVE, DATE_BEFORE
    - Comparison failures (e.g., trying to compare a non-numeric string to MIN/MAX)
    - Empty options for ENUM_MEMBER (should be handled gracefully)
  </action>
  <verify>
    <automated>pytest tests/test_validation.py -v --cov=schemakernel/validation.py --cov-report=term-missing</automated>
  </verify>
  <done>Answer validation and coercion paths are 100% covered</done>
</task>

<task type="auto">
  <name>Task 3: Policy Invariant Tests</name>
  <files>tests/test_models.py</files>
  <action>
    Add a test case to verify that `PolicyConfig` raises a `ValueError` if `baseline_questions` contains duplicate field keys.
  </action>
  <verify>
    <automated>pytest tests/test_models.py -v --cov=schemakernel/policy.py</automated>
  </verify>
  <done>PolicyConfig validation for unique baseline keys is covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Planner -> ValidationEngine | Planner output is untrusted; validation must enforce safety |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | ui_props | mitigate | validate ui_props against forbidden patterns |
| T-01-02 | Information Disclosure | Prohibited Topics | mitigate | scan rationale and next_prompt_context for prohibited topics |
</threat_model>

<success_criteria>
`schemakernel/validation.py` and `schemakernel/policy.py` reach 100% coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-01-SUMMARY.md`
</output>
