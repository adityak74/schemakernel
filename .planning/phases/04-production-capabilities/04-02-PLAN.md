---
phase: 04-production-capabilities
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [schemakernel/telemetry/logging.py, schemakernel/telemetry/tracing.py, tests/test_telemetry.py]
autonomous: true
requirements: [PROD-03]
must_haves:
  truths:
    - "Logs are emitted in JSON format"
    - "Instructor calls create OpenTelemetry spans"
    - "Trace data includes session_id"
  artifacts:
    - path: "schemakernel/telemetry/logging.py"
      provides: "Structured logging"
    - path: "schemakernel/telemetry/tracing.py"
      provides: "OTel instrumentation"
  key_links:
    - from: "schemakernel/telemetry/tracing.py"
      to: "instructor"
      via: "InstructorInstrumentor"
---

<objective>
Implement observability and telemetry to provide production-grade logging and distributed tracing.

Purpose: Enable debugging and performance monitoring of LLM-driven workflows.
Output: structlog-based structured logging and OpenTelemetry instrumentation for instructor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-production-capabilities/04-RESEARCH.md
@schemakernel/planner.py
@schemakernel/workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup structured logging with structlog</name>
  <files>schemakernel/telemetry/logging.py</files>
  <action>
    - Create `schemakernel/telemetry/logging.py`.
    - Configure `structlog` with standard production processors (JSON renderer, timestamp, log level, callsite).
    - Provide a helper function `get_logger(name)` that returns a configured structlog logger.
    - Ensure logging can be configured via environment variables (e.g., LOG_LEVEL).
  </action>
  <verify>
    <automated>python -c "from schemakernel.telemetry.logging import get_logger; logger = get_logger('test'); logger.info('hello', key='value')"</automated>
  </verify>
  <done>Structured logging configured and ready for use.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate OpenTelemetry and Instructor instrumentation</name>
  <files>schemakernel/telemetry/tracing.py</files>
  <action>
    - Create `schemakernel/telemetry/tracing.py`.
    - Setup OpenTelemetry `TracerProvider`.
    - Implement `setup_tracing(service_name: str)` function.
    - Use `InstructorInstrumentor().instrument()` to automatically trace `instructor` calls.
    - Ensure traces include `session_id` if provided in the context.
  </action>
  <verify>
    <automated>python -c "from schemakernel.telemetry.tracing import setup_tracing; setup_tracing('test-service')"</automated>
  </verify>
  <done>OpenTelemetry instrumentation for instructor calls implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Verification tests for telemetry</name>
  <files>tests/test_telemetry.py</files>
  <action>
    - Create `tests/test_telemetry.py`.
    - Test that `get_logger` returns a logger that produces JSON output (mock standard out).
    - Test that `instructor` calls trigger traces by using an OTel `InMemorySpanExporter`.
    - Verify that `session_id` is propagated to spans if possible.
  </action>
  <verify>
    <automated>pytest tests/test_telemetry.py</automated>
  </verify>
  <done>Telemetry verified with automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Telemetry → Collector: Traces and logs cross to external observability platforms.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Logs/Traces | mitigate | Redact PII from logs and traces before export (linked to Plan 04). |
| T-04-02-02 | Denial of Service | OTel Export | mitigate | Use asynchronous batching for span export to avoid blocking the main thread. |
</threat_model>

<success_criteria>
- structlog produces JSON logs suitable for ELK/Datadog.
- Instructor calls are visible in OpenTelemetry traces.
- Telemetry configuration is manageable via environment variables.
</success_criteria>

<output>
After completion, create .planning/phases/04-production-capabilities/04-02-SUMMARY.md
</output>
