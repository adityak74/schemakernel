---
phase: 04-production-capabilities
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [schemakernel/compliance/redaction.py, schemakernel/storage/redaction_wrapper.py, tests/test_redaction.py]
autonomous: true
requirements: [PROD-04]
must_haves:
  truths:
    - "PII in text is replaced by masks (e.g., <PERSON>)"
    - "Storage wrapper redacts data before persistence"
    - "Non-sensitive data is preserved during redaction"
  artifacts:
    - path: "schemakernel/compliance/redaction.py"
      provides: "PII Detection/Redaction"
  key_links:
    - from: "schemakernel/storage/redaction_wrapper.py"
      to: "schemakernel/compliance/redaction.py"
      via: "RedactionEngine"
---

<objective>
Implement PII redaction capabilities using Microsoft Presidio to ensure data compliance before storage.

Purpose: Protect sensitive user information (names, emails, phones) from being stored in cleartext.
Output: Redaction engine and a storage wrapper to automate redaction.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-production-capabilities/04-RESEARCH.md
@schemakernel/store.py
@schemakernel/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Redaction Engine with Microsoft Presidio</name>
  <files>schemakernel/compliance/redaction.py</files>
  <action>
    - Create `schemakernel/compliance/redaction.py`.
    - Initialize `AnalyzerEngine` and `AnonymizerEngine` from `presidio_analyzer` and `presidio_anonymizer`.
    - Implement `RedactionEngine` class with a `redact_text(text: str) -> str` method.
    - Implement `redact_data(data: Any) -> Any` that recursively redacts strings in dictionaries/lists.
    - Support configuring the list of entities to redact (e.g., PERSON, EMAIL_ADDRESS, PHONE_NUMBER).
  </action>
  <verify>
    <automated>python -c "from schemakernel.compliance.redaction import RedactionEngine; engine = RedactionEngine(); print(engine.redact_text('My name is John Doe'))"</automated>
  </verify>
  <done>Redaction engine implemented and verified with basic strings.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Redacting Storage Wrapper</name>
  <files>schemakernel/storage/redaction_wrapper.py</files>
  <action>
    - Create `schemakernel/storage/redaction_wrapper.py`.
    - Implement `RedactingStorageWrapper` using the decorator pattern to wrap any `StorageBackend`.
    - Override `save_state` to redact `answers` and `audit_log` within `SchemaState` before calling the wrapped backend's `save_state`.
    - Ensure other methods (`load_state`, `save_trace`, etc.) are correctly delegated.
  </action>
  <verify>
    <automated>python -c "from schemakernel.storage.redaction_wrapper import RedactingStorageWrapper; print('Import successful')"</automated>
  </verify>
  <done>Storage wrapper created to automate redaction during persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Redaction verification tests</name>
  <files>tests/test_redaction.py</files>
  <action>
    - Create `tests/test_redaction.py`.
    - Test `RedactionEngine` with various PII types (emails, phone numbers, US SSN, etc.).
    - Test `RedactingStorageWrapper` with an `InMemoryStore` to verify that data is redacted before hitting the inner store.
    - Verify that non-PII data remains untouched.
  </action>
  <verify>
    <automated>pytest tests/test_redaction.py</automated>
  </verify>
  <done>Redaction logic verified with comprehensive PII samples.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Application → Storage: PII is redacted at this boundary.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | Database | mitigate | Presidio redaction removes PII before it reaches the persistence layer. |
| T-04-04-02 | Information Disclosure | Redactor False Negatives | mitigate | Allow configuration of 'strict' modes and custom PII patterns in the Analyzer. |
</threat_model>

<success_criteria>
- RedactionEngine correctly masks PII in text and complex data structures.
- RedactingStorageWrapper transparently redacts session state before storage.
- Tests confirm that no cleartext PII is saved when the wrapper is active.
</success_criteria>

<output>
After completion, create .planning/phases/04-production-capabilities/04-04-SUMMARY.md
</output>
