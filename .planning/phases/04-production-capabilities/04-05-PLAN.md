---
phase: 04-production-capabilities
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [Dockerfile, .dockerignore, deployment/helm/schemakernel/Chart.yaml, deployment/helm/schemakernel/values.yaml, deployment/helm/schemakernel/templates/deployment.yaml]
autonomous: true
requirements: [PROD-05]
must_haves:
  truths:
    - "Docker image builds without error"
    - "Helm chart renders valid Kubernetes manifests"
    - "Deployment variables are documented"
  artifacts:
    - path: "Dockerfile"
      provides: "Containerization"
    - path: "deployment/helm/schemakernel/Chart.yaml"
      provides: "K8s orchestration"
  key_links:
    - from: "deployment/helm/schemakernel/templates/deployment.yaml"
      to: "Dockerfile"
      via: "Image reference"
---

<objective>
Provide enterprise-grade deployment artifacts including Docker images and Helm charts for Kubernetes.

Purpose: Standardize the deployment of SchemaKernel applications in production environments.
Output: Multi-stage Dockerfile and Helm v3/v4 charts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-production-capabilities/04-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-stage Dockerfile and .dockerignore</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    - Create a multi-stage `Dockerfile`.
    - Stage 1 (builder): Install build dependencies and create a virtual environment.
    - Stage 2 (runtime): Use a slim base image (e.g., python:3.11-slim), copy the virtual environment, and install only runtime dependencies.
    - Ensure the image runs as a non-root user for security.
    - Create `.dockerignore` to exclude development artifacts, tests, and secrets.
    - Set default entrypoint to run a Streamlit example or a generic worker.
  </action>
  <verify>
    <automated>docker build -t schemakernel:latest .</automated>
  </verify>
  <done>Production-ready Docker image configuration completed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Helm v3 Chart for SchemaKernel</name>
  <files>deployment/helm/schemakernel/Chart.yaml, deployment/helm/schemakernel/values.yaml, deployment/helm/schemakernel/templates/deployment.yaml</files>
  <action>
    - Create standard Helm chart structure in `deployment/helm/schemakernel/`.
    - Define `Chart.yaml` with versioning and metadata.
    - Define `values.yaml` with configurable options for:
        - Image repository and tag.
        - Environment variables for Storage (SQL/DynamoDB), Telemetry (OTel), and Redaction.
        - Kubernetes resource limits (CPU/Memory).
        - Service type and port.
    - Implement `deployment.yaml` template using the configured values.
  </action>
  <verify>
    <automated>helm lint deployment/helm/schemakernel/</automated>
  </verify>
  <done>Helm chart created and validated with linting.</done>
</task>

<task type="auto">
  <name>Task 3: Document deployment patterns and environment configuration</name>
  <files>README.md</files>
  <action>
    - Update `README.md` or create `DEPLOYMENT.md` with instructions on how to use the Docker image and Helm chart.
    - Document all critical environment variables (e.g., `DATABASE_URL`, `DYNAMODB_TABLE`, `OTEL_EXPORTER_OTLP_ENDPOINT`, `PRESIDIO_ENTITIES`).
    - Provide an example `values.yaml` for a production setup.
  </action>
  <verify>
    <automated>grep "Deployment" README.md</automated>
  </verify>
  <done>Deployment documentation completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Container → Host: Restricted via non-root user.
- Pod → Kubernetes API: Restricted via RBAC (out of scope for chart, but following best practices).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Elevation of Privilege | Docker Container | mitigate | Run as non-root user; use minimal base image to reduce attack surface. |
| T-04-05-02 | Resource Exhaustion | Kubernetes Pod | mitigate | Define CPU and Memory limits in Helm values to prevent one pod from starving the node. |
</threat_model>

<success_criteria>
- Docker image builds successfully and is minimal in size.
- Helm chart passes `helm lint` and correctly renders templates with `helm template`.
- Deployment documentation clearly explains how to configure all production features.
</success_criteria>

<output>
After completion, create .planning/phases/04-production-capabilities/04-05-SUMMARY.md
</output>
