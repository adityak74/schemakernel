---
phase: 02-streamlit-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemakernel/adapters/__init__.py
  - schemakernel/adapters/streamlit.py
  - tests/test_streamlit_store.py
autonomous: true
requirements:
  - ST-02
must_haves:
  truths:
    - "StreamlitSessionStore implements the StorageBackend interface"
    - "SchemaState is persisted in st.session_state using a configurable prefix"
    - "Loading a missing session raises SessionNotFound"
  artifacts:
    - path: "schemakernel/adapters/streamlit.py"
      provides: "StreamlitSessionStore implementation"
    - path: "tests/test_streamlit_store.py"
      provides: "Unit tests for StreamlitSessionStore"
  key_links:
    - from: "schemakernel/adapters/streamlit.py"
      to: "st.session_state"
      via: "dictionary access"
---

<objective>
Implement the `StreamlitSessionStore` to enable workflow state persistence using Streamlit's native `st.session_state`.

Purpose: Provides an idiomatic way to handle session state in Streamlit apps without requiring an external database.
Output: `StreamlitSessionStore` class in `schemakernel/adapters/streamlit.py`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@schemakernel/store.py
@schemakernel/models.py
@tests/test_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create adapters package and StreamlitSessionStore</name>
  <files>schemakernel/adapters/__init__.py, schemakernel/adapters/streamlit.py</files>
  <action>
    - Create `schemakernel/adapters/` directory.
    - Create empty `__init__.py` in `schemakernel/adapters/`.
    - Create `schemakernel/adapters/streamlit.py`.
    - Implement `StreamlitSessionStore` inheriting from `StorageBackend` (from `schemakernel.store`).
    - Use `import streamlit as st` inside methods to avoid hard dependency at module load if possible, or handle the dependency gracefully.
    - Implement all abstract methods: `save_state`, `load_state`, `delete_state`, `save_trace`, `list_traces`, `save_outcome`, `load_outcome`.
    - Use a `key_prefix` (default "sk_") to avoid collisions in `st.session_state`.
  </action>
  <verify>
    <automated>python -c "from schemakernel.adapters.streamlit import StreamlitSessionStore; print('Import OK')"</automated>
  </verify>
  <done>StreamlitSessionStore implemented and imports without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test StreamlitSessionStore with mocked session_state</name>
  <files>tests/test_streamlit_store.py</files>
  <behavior>
    - Test saving and loading SchemaState.
    - Test SessionNotFound exception for missing keys.
    - Test saving/listing traces and outcomes.
    - Test deletion.
    - Ensure deep copies are used for state storage/retrieval per D-03.
  </behavior>
  <action>
    - Create `tests/test_streamlit_store.py`.
    - Use `unittest.mock.patch` to mock `streamlit.session_state` as a simple dict.
    - Replicate test cases from `tests/test_store.py` adapted for the Streamlit backend.
  </action>
  <verify>
    <automated>pytest tests/test_streamlit_store.py</automated>
  </verify>
  <done>StreamlitSessionStore verified with 100% coverage of store operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application → st.session_state | Internal state persistence |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Session State | mitigate | Use Pydantic models for validation on load; assume st.session_state is secure within the Streamlit runtime environment. |
</threat_model>

<verification>
Ensure `StreamlitSessionStore` correctly maps all `StorageBackend` methods to `st.session_state` keys and handles the `SessionNotFound` error.
</verification>

<success_criteria>
- `StreamlitSessionStore` implemented in `schemakernel/adapters/streamlit.py`.
- Tests pass using mocked Streamlit session state.
- Compliance with `StorageBackend` interface.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streamlit-adapter/02-01-SUMMARY.md`
</output>
