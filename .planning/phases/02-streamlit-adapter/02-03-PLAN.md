---
phase: 02-streamlit-adapter
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - schemakernel/__init__.py
  - examples/streamlit_app.py
  - README.md
autonomous: false
requirements:
  - ST-04
must_haves:
  truths:
    - "A working Streamlit example exists that demonstrates the adaptive loop"
    - "README.md contains instructions for using the Streamlit adapter"
    - "StreamlitSessionStore and StreamlitFormAdapter are exported in the top-level package"
  artifacts:
    - path: "examples/streamlit_app.py"
      provides: "Reference implementation"
    - path: "README.md"
      provides: "Developer documentation"
  key_links:
    - from: "examples/streamlit_app.py"
      to: "schemakernel.adapters.streamlit"
      via: "import"
---

<objective>
Provide a complete reference implementation and developer documentation for the Streamlit adapter, and expose it via the public API.

Purpose: Makes the new Streamlit capabilities discoverable and easy to adopt.
Output: Working example app, updated README, and updated package exports.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@schemakernel/__init__.py
@schemakernel/adapters/streamlit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package exports</name>
  <files>schemakernel/__init__.py</files>
  <action>
    - Export `StreamlitSessionStore` and `StreamlitFormAdapter` in `schemakernel/__init__.py`.
    - Update `__all__` list to include these new symbols.
    - Ensure imports are guarded or handled if `streamlit` is not installed (optional, given it's a first-party adapter).
  </action>
  <verify>
    <automated>python -c "from schemakernel import StreamlitSessionStore, StreamlitFormAdapter; print('Export OK')"</automated>
  </verify>
  <done>Streamlit adapters available at top-level import.</done>
</task>

<task type="auto">
  <name>Task 2: Create reference Streamlit application</name>
  <files>examples/streamlit_app.py</files>
  <action>
    - Create `examples/` directory if it doesn't exist.
    - Implement a simple "Patient Intake" adaptive form in `examples/streamlit_app.py`.
    - The app should:
        1. Initialize a `WorkflowStateMachine` with a `StreamlitSessionStore`.
        2. Use `StreamlitFormAdapter` to render fields.
        3. Show a "Thinking..." spinner while running the planner turn.
        4. Handle completion by showing the final captured data.
    - Use a Mock Planner or a simple hardcoded logic if an LLM key is not available for testing.
  </action>
  <verify>
    <automated>python -m py_compile examples/streamlit_app.py</automated>
  </verify>
  <done>Reference application created and syntactically correct.</done>
</task>

<task type="auto">
  <name>Task 3: Update README and Documentation</name>
  <files>README.md</files>
  <action>
    - Add a "Streamlit Support" section to `README.md`.
    - Provide a quick start code snippet using `StreamlitFormAdapter` and `StreamlitSessionStore`.
    - Mention how to run the example app.
  </action>
  <verify>
    <automated>grep -i "Streamlit" README.md</automated>
  </verify>
  <done>Documentation updated with Streamlit integration guide.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of example app</name>
  <action>Manual interaction with the Streamlit example app to ensure UI feel and flow are correct.</action>
  <what-built>Full Streamlit adapter integration with a working example app.</what-built>
  <how-to-verify>
    1. Run the example app: `streamlit run examples/streamlit_app.py`
    2. Interact with the form: fill out fields, click submit.
    3. Verify that the schema evolves (if using a dynamic planner) and that state is preserved correctly.
    4. Check the final output on completion.
  </how-to-verify>
  <verify>Manual verification successful</verify>
  <done>Example app verified by human</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Developer → Example Code | Usage of patterns |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Example App | mitigate | Ensure example app does not leak secrets or PII; use placeholders for LLM keys. |
</threat_model>

<verification>
Ensure the example app is fully functional and the README accurately describes the integration steps.
</verification>

<success_criteria>
- Example app runs without errors.
- Documentation is clear and correct.
- Adapters are easily accessible to library users.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streamlit-adapter/02-03-SUMMARY.md`
</output>
