---
phase: 03-js-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [js-sdk/package.json, js-sdk/tsconfig.json, js-sdk/src/models.ts, vectors/loader.py, js-sdk/src/vectors.ts]
autonomous: true
requirements: [JS-MOD]
must_haves:
  truths:
    - "js-sdk directory is a valid TypeScript project"
    - "Zod schemas match Python model requirements"
    - "Shared vectors are readable by both vitest and pytest"
  artifacts:
    - path: "js-sdk/src/models.ts"
      provides: "Canonical Zod models"
    - path: "vectors/schema_vectors.json"
      provides: "Shared test data"
  key_links:
    - from: "js-sdk/src/models.ts"
      to: "schemakernel/models.py"
      via: "manual parity check"
---

<objective>
Initialize the JavaScript SDK project and port the canonical models with Zod to ensure type-safe parity with the Python implementation.

Purpose: Foundation for the JS SDK, ensuring models are identical across runtimes.
Output: TypeScript project structure and Zod schemas for all SchemaKernel models.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-sdk/03-RESEARCH.md
@schemakernel/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization</name>
  <files>js-sdk/package.json, js-sdk/tsconfig.json, js-sdk/tsup.config.ts, js-sdk/vitest.config.ts, js-sdk/src/index.ts</files>
  <action>
    Initialize the `js-sdk` directory as a TypeScript project.
    1. Create `package.json` with dependencies: `zod`, `dayjs`, `@instructor-ai/instructor`, `openai`, `express`, `survey-core`.
    2. Add devDependencies: `typescript`, `vitest`, `tsup`, `@types/express`, `@types/node`.
    3. Configure `tsconfig.json` for ESM output and strict type checking.
    4. Set up `tsup.config.ts` for bundling the library.
    5. Set up `vitest.config.ts` for TypeScript-native testing.
    6. Create a placeholder `src/index.ts`.
  </action>
  <verify>
    <automated>cd js-sdk && npm install && npx tsc --noEmit</automated>
  </verify>
  <done>Project structure created, dependencies installed, and TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Shared Vector Infrastructure</name>
  <files>vectors/schema_vectors.json, tests/conftest.py, js-sdk/src/vectors.ts</files>
  <action>
    Create a shared directory for JSON-based test vectors to ensure multi-runtime parity.
    1. Create a `vectors/` directory at the project root.
    2. Add `vectors/schema_vectors.json` with initial test cases for `FieldDefinition` and `ValidatorRule`.
    3. Implement `js-sdk/src/vectors.ts` utility to load and parse these JSON vectors for Vitest.
    4. Update `tests/conftest.py` or create a utility in Python to load these vectors for Pytest.
  </action>
  <verify>
    <automated>ls vectors/schema_vectors.json && cd js-sdk && npx vitest run tests/unit/vectors.test.ts --passWithNoTests</automated>
  </verify>
  <done>Vectors directory created and loader utilities available in both JS and Python.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port Canonical Models</name>
  <files>js-sdk/src/models.ts, js-sdk/tests/unit/models.test.ts</files>
  <behavior>
    - Ensure `FieldType`, `ValidatorType`, `ActionType`, `WorkflowStage`, `ConditionOperator` match Python enums exactly.
    - `FieldDefinitionSchema` must validate field keys (regex) and ui_props (forbidden patterns).
    - `ValidatorRuleSchema` must require `value` for parameterized validators (MIN, MAX, etc.).
    - `PlannerResponseSchema` must validate that actions refer to defined fields.
  </behavior>
  <action>
    Implement all models from `schemakernel/models.py` using `zod`.
    1. Port all enums.
    2. Implement `ValidatorRule`, `Condition`, `ConditionalLogic`.
    3. Implement `FieldDefinition` with `.refine()` calls mirroring Python's `model_validator(mode="after")` logic.
    4. Implement `PlannerAction`, `PlannerResponse`, `SchemaState`.
    5. Export all types using `z.infer`.
  </action>
  <verify>
    <automated>cd js-sdk && npx vitest tests/unit/models.test.ts</automated>
  </verify>
  <done>Models implemented in TypeScript and passing validation tests mirroring Python behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner Output → SDK | Untrusted LLM output crosses into the SDK model layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `FieldDefinition` | mitigate | Use Zod's `refine` to enforce `_FORBIDDEN_UI_PATTERNS` parity. |
| T-03-01-02 | Tampering | `PlannerResponse` | mitigate | Strict schema validation ensuring actions match defined fields. |
</threat_model>

<verification>
Verify project initialization by running `npm install`.
Verify model parity by running Vitest with shared vectors.
</verification>

<success_criteria>
- `js-sdk/` initialized with TypeScript.
- `js-sdk/src/models.ts` implements all canonical models.
- All model tests pass, including parity refinements.
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-sdk/03-01-SUMMARY.md`
</output>
