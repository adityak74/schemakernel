---
phase: 03-js-sdk
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [js-sdk/src/planner.ts, js-sdk/src/workflow.ts]
autonomous: true
requirements: [JS-CORE]
must_haves:
  truths:
    - "PlannerClient successfully returns structured objects"
    - "Workflow transitions follow the defined state machine"
  artifacts:
    - path: "js-sdk/src/workflow.ts"
      provides: "WorkflowStateMachine"
    - path: "js-sdk/src/planner.ts"
      provides: "PlannerClient"
  key_links:
    - from: "js-sdk/src/workflow.ts"
      to: "js-sdk/src/planner.ts"
      via: "runPlannerTurn"
---

<objective>
Implement the core orchestration logic, including the AI planner client and the workflow state machine, to enable turn-based form evolution in JavaScript.

Purpose: Orchestration of AI planning and state transitions.
Output: Planner client and WorkflowStateMachine in TypeScript.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-sdk/03-02-SUMMARY.md
@schemakernel/planner.py
@schemakernel/workflow.py
</context>

<interfaces>
From js-sdk/src/models.ts:
```typescript
export interface PlannerResponse { ... }
export interface SchemaState { ... }
```
From js-sdk/src/validation.ts:
```typescript
export class ValidationEngine {
  validatePlannerResponse(response: PlannerResponse, state: SchemaState): ValidationResult;
  validateAnswer(field: FieldDefinition, value: any): ValidationResult;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Planner Client</name>
  <files>js-sdk/src/planner.ts, js-sdk/tests/unit/planner.test.ts</files>
  <action>
    Implement `PlannerClient` using `@instructor-ai/instructor`.
    1. Create `src/planner.ts` with a class mirroring `schemakernel/planner.py`.
    2. Support OpenAI and Anthropic providers (if supported by `instructor-js`).
    3. Implement the `call` method to fetch structured `PlannerResponse`.
    4. Ensure system prompt and messages construction matches the Python logic exactly.
  </action>
  <verify>
    <automated>cd js-sdk && npx vitest tests/unit/planner.test.ts</automated>
  </verify>
  <done>Planner client implemented with structured output support via instructor-js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workflow State Machine</name>
  <files>js-sdk/src/workflow.ts, js-sdk/tests/unit/workflow.test.ts</files>
  <behavior>
    - `startSession`: Must initialize state with baseline questions from policy.
    - `submitAnswer`: Must validate and persist answers, updating audit log.
    - `runPlannerTurn`: Must build identical system prompts to Python, call planner, validate response, and apply all action types (ADD, UPDATE, REMOVE, etc.).
    - `_evaluateCondition`: Must recursive evaluate `ConditionalLogic` for `getNextFields`.
  </behavior>
  <action>
    Implement `WorkflowStateMachine` class in `src/workflow.ts`.
    1. Port `WorkflowStateMachine` and all its methods from `workflow.py`.
    2. Ensure `_applyActions` logic matches Python exactly (including HIDE using `ALWAYS_HIDDEN`).
    3. Implement `_evaluateCondition` for field visibility logic.
  </action>
  <verify>
    <automated>cd js-sdk && npx vitest tests/unit/workflow.test.ts</automated>
  </verify>
  <done>Workflow state machine implemented with turn-based evolution and condition evaluation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → LLM API | System prompts and user answers sent to external AI provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | `WorkflowStateMachine` | mitigate | Ensure system prompt construction handles prohibited topics and glossary correctly. |
| T-03-03-02 | Denial of Service | `WorkflowStateMachine` | mitigate | Enforce `max_turns` limit before running a planner turn. |
</threat_model>

<verification>
Verify workflow transitions (initialized -> collecting -> complete) via unit tests.
Verify condition evaluation logic with complex nested AND/OR groups.
</verification>

<success_criteria>
- `PlannerClient` successfully extracts structured JSON.
- `WorkflowStateMachine` manages full session lifecycle.
- State transitions and action application match Python behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-sdk/03-03-SUMMARY.md`
</output>
