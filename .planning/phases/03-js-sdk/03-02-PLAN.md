---
phase: 03-js-sdk
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [js-sdk/src/store.ts, js-sdk/src/policy.ts, js-sdk/src/validation.ts, js-sdk/src/exceptions.ts]
autonomous: true
requirements: [JS-CORE]
must_haves:
  truths:
    - "Validation logic for answers matches Python implementation"
    - "InMemoryStore performs deep copies"
  artifacts:
    - path: "js-sdk/src/validation.ts"
      provides: "ValidationEngine"
    - path: "js-sdk/src/store.ts"
      provides: "StorageBackend & InMemoryStore"
  key_links:
    - from: "js-sdk/src/validation.ts"
      to: "dayjs"
      via: "date coercion"
---

<objective>
Implement the foundational logic of the JS SDK, including storage abstractions, policy configuration, and the validation engine, ensuring strict parity with the Python core.

Purpose: Core logic for state management and validation.
Output: Storage, Policy, and Validation modules in TypeScript.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-sdk/03-01-SUMMARY.md
@schemakernel/store.py
@schemakernel/policy.py
@schemakernel/validation.py
@schemakernel/exceptions.py
</context>

<interfaces>
From js-sdk/src/models.ts:
```typescript
export interface SchemaState { ... }
export interface FieldDefinition { ... }
export interface ValidatorRule { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Exceptions, Policy, and Storage</name>
  <files>js-sdk/src/exceptions.ts, js-sdk/src/policy.ts, js-sdk/src/store.ts</files>
  <action>
    Port the foundational infrastructure modules.
    1. Implement `src/exceptions.ts` with classes mirroring `schemakernel/exceptions.py`.
    2. Implement `src/policy.ts` with Zod-backed `PolicyConfig` mirroring `schemakernel/policy.py`.
    3. Implement `src/store.ts` with `StorageBackend` interface and `InMemoryStore` class. Ensure `InMemoryStore` uses deep copies for persistence and retrieval to prevent state mutation issues (per D-03).
  </action>
  <verify>
    <automated>cd js-sdk && npx vitest tests/unit/store.test.ts</automated>
  </verify>
  <done>Exceptions, Policy, and Store implemented with deep-copy persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validation Engine</name>
  <files>js-sdk/src/validation.ts, js-sdk/tests/unit/validation.test.ts</files>
  <behavior>
    - `validatePlannerResponse`: Must enforce allowlists for actions/fields/validators and policy-based max_fields.
    - `validateAnswer`: Must handle type coercion exactly like Python (especially Booleans and Dates using `dayjs`).
    - `_checkRule`: Must implement all `ValidatorType` checks with identical logic to `validation.py`, including regex fullmatch parity.
  </behavior>
  <action>
    Implement `ValidationEngine` class in `src/validation.ts`.
    1. Port `ValidationResult` and `ValidationEngine`.
    2. Implement `validatePlannerResponse` with all security and policy checks.
    3. Implement `validateAnswer` with coercion for all `FieldType`s.
    4. Use `dayjs` for date/datetime coercion and comparisons to match Python's `dateutil`.
  </action>
  <verify>
    <automated>cd js-sdk && npx vitest tests/unit/validation.test.ts</automated>
  </verify>
  <done>Validation engine implemented with full feature parity for all validator types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Validation | External answers are coerced and validated against schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `InMemoryStore` | mitigate | Implement deep copies on save/load to prevent reference-based state corruption. |
| T-03-02-02 | Information Disclosure | `ValidationEngine` | mitigate | Ensure prohibited topics check matches policy exactly. |
</threat_model>

<verification>
Verify storage deep-copy behavior with unit tests.
Verify validation parity by running answer validation tests with edge cases (booleans, dates, regex).
</verification>

<success_criteria>
- `StorageBackend` and `InMemoryStore` implemented.
- `ValidationEngine` implements all canonical validation rules.
- Coercion logic matches Python implementation (tested via unit tests).
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-sdk/03-02-SUMMARY.md`
</output>
