---
phase: 03-js-sdk
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified: [js-sdk/src/adapters/surveyjs.ts, js-sdk/src/middleware/express.ts, js-sdk/src/index.ts]
autonomous: true
requirements: [JS-SUR, JS-MID]
must_haves:
  truths:
    - "SurveyJS adapter output is compatible with SurveyJS v2"
    - "Express middleware handles session lifecycle via API"
  artifacts:
    - path: "js-sdk/src/adapters/surveyjs.ts"
      provides: "SurveyJS mapping logic"
    - path: "js-sdk/src/middleware/express.ts"
      provides: "Express API endpoints"
  key_links:
    - from: "js-sdk/src/adapters/surveyjs.ts"
      to: "SchemaState"
      via: "state-to-json mapping"
---

<objective>
Deliver integration adapters for SurveyJS and Express.js to enable full-stack SchemaKernel applications in the JavaScript ecosystem.

Purpose: Frontend and Backend integration support.
Output: SurveyJS adapter and Express middleware in TypeScript.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-sdk/03-03-SUMMARY.md
@.planning/phases/03-js-sdk/03-RESEARCH.md
</context>

<interfaces>
From js-sdk/src/models.ts:
```typescript
export interface SchemaState { ... }
export interface FieldDefinition { ... }
```
From js-sdk/src/workflow.ts:
```typescript
export class WorkflowStateMachine {
  startSession(sessionId?: string): SchemaState;
  submitAnswer(sessionId: string, fieldKey: string, value: any): ValidationResult;
  runPlannerTurn(sessionId: string): Promise<SchemaState>;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SurveyJS Adapter</name>
  <files>js-sdk/src/adapters/surveyjs.ts, js-sdk/tests/unit/surveyjs.test.ts</files>
  <behavior>
    - `mapToSurveyJS`: Must convert `SchemaState` to valid SurveyJS JSON format.
    - `FieldType` mapping: `text` -> `text`, `textarea` -> `comment`, `select` -> `dropdown`, etc.
    - `ConditionalLogic` mapping: Must recursive map to SurveyJS `visibleIf` expression strings (e.g. `{field} = 'value'`).
    - Use `anyof` for `ConditionOperator.IN`.
  </behavior>
  <action>
    Implement `SurveyJSAdapter` in `src/adapters/surveyjs.ts`.
    1. Create a mapper function that takes `SchemaState` and returns a SurveyJS JSON object.
    2. Implement recursive expression mapping for `visibleIf`.
    3. Ensure all basic field types and validators are correctly mapped to SurveyJS equivalents.
  </action>
  <verify>
    <automated>cd js-sdk && npx vitest tests/unit/surveyjs.test.ts</automated>
  </verify>
  <done>SurveyJS adapter implemented, converting SchemaState to fully functional SurveyJS JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Express Middleware</name>
  <files>js-sdk/src/middleware/express.ts, js-sdk/tests/unit/middleware.test.ts, js-sdk/src/index.ts</files>
  <action>
    Implement Express.js middleware for easy orchestration.
    1. Create `src/middleware/express.ts` providing a router or middleware functions.
    2. Implement endpoints for `POST /session` (start), `POST /answer` (submit), and `POST /plan` (trigger turn).
    3. Ensure error handling for `SessionNotFound`, `ValidationErrors`, and `PlannerErrors`.
    4. Export all public components from `src/index.ts`.
  </action>
  <verify>
    <automated>cd js-sdk && npx vitest tests/unit/middleware.test.ts</automated>
  </verify>
  <done>Express middleware implemented, providing standard API endpoints for SDK orchestration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Express API | External requests from browsers or mobile apps |
| SDK → SurveyJS JSON | Schema data serialized for client-side rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | `Express Middleware` | mitigate | Use Zod to validate all incoming request bodies before passing to SDK. |
| T-03-04-02 | XSS | `SurveyJS Adapter` | mitigate | Ensure `ui_props` are sanitized in the adapter before being embedded in JSON. |
</threat_model>

<verification>
Verify SurveyJS JSON output against sample schemas.
Verify Express API endpoints using `supertest` or equivalent.
</verification>

<success_criteria>
- `SurveyJSAdapter` produces valid SurveyJS JSON with working conditional logic.
- `Express` middleware provides a functional API for form orchestration.
- Public API exported correctly from `src/index.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-sdk/03-04-SUMMARY.md`
</output>
