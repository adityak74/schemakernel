---
phase: 03-js-sdk
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified: [vectors/validation_vectors.json, vectors/workflow_vectors.json, tests/test_parity.py, js-sdk/tests/parity/core.test.ts]
autonomous: true
requirements: [JS-PAR]
must_haves:
  truths:
    - "100% parity achieved on shared validation vectors"
    - "Build artifacts are correctly bundled and typed"
  artifacts:
    - path: "tests/test_parity.py"
      provides: "Python side parity test"
    - path: "js-sdk/tests/parity/core.test.ts"
      provides: "JS side parity test"
  key_links:
    - from: "tests/test_parity.py"
      to: "vectors/"
      via: "loading shared vectors"
---

<objective>
Establish a rigorous multi-runtime parity testing suite using shared JSON vectors to guarantee that the JavaScript SDK behaves identically to the Python core.

Purpose: Guarantee cross-runtime consistency.
Output: Shared test vectors and parity test suites in both Python and TypeScript.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-sdk/03-04-SUMMARY.md
@tests/test_validation.py
@tests/test_workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Shared Parity Vectors</name>
  <files>vectors/validation_vectors.json, vectors/workflow_vectors.json, vectors/condition_vectors.json</files>
  <action>
    Create comprehensive JSON vectors for core logic parity.
    1. `validation_vectors.json`: Include all edge cases from `tests/test_validation.py` (coercion, rule checks, forbidden patterns).
    2. `workflow_vectors.json`: Include state transition cases (Action + State -> New State).
    3. `condition_vectors.json`: Include complex `ConditionalLogic` evaluation cases.
  </action>
  <verify>
    <automated>ls vectors/*.json</automated>
  </verify>
  <done>Comprehensive JSON vectors generated for cross-runtime testing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Parity Tests</name>
  <files>tests/test_parity.py, js-sdk/tests/parity/core.test.ts</files>
  <behavior>
    - Python Parity: Load all JSON vectors and run `pytest` against `ValidationEngine` and `WorkflowStateMachine`.
    - JS Parity: Load identical JSON vectors and run `vitest` against `ValidationEngine` and `WorkflowStateMachine`.
    - Both suites must pass 100% of the shared vectors.
  </behavior>
  <action>
    Implement the parity test runners in both environments.
    1. Implement `tests/test_parity.py` in Python using `pytest.mark.parametrize` over the JSON vectors.
    2. Implement `js-sdk/tests/parity/core.test.ts` in TypeScript using `test.each()` over the same JSON vectors.
    3. Ensure Date/Time serialization is handled consistently (ISO 8601).
  </action>
  <verify>
    <automated>pytest tests/test_parity.py && cd js-sdk && npx vitest tests/parity/core.test.ts</automated>
  </verify>
  <done>Parity tests implemented and passing in both Python and JavaScript.</done>
</task>

<task type="auto">
  <name>Task 3: Build & Finalize</name>
  <files>js-sdk/package.json, js-sdk/dist/</files>
  <action>
    Finalize the SDK for distribution.
    1. Run `npm run build` to generate the `dist/` folder using `tsup`.
    2. Verify that the generated type definitions are correct.
    3. Update `package.json` entry points (`main`, `module`, `types`).
  </action>
  <verify>
    <automated>cd js-sdk && npm run build && ls dist/index.js dist/index.d.ts</automated>
  </verify>
  <done>JS SDK built and ready for distribution with verified type definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Data → SDK | Test vectors used to verify SDK logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | `Test Vectors` | accept | Test vectors are internal artifacts; risk of tampering is low as they are part of the codebase. |
		</threat_model>

<verification>
Run both Python and JS parity test suites.
Verify the build output of the JS SDK.
</verification>

<success_criteria>
- 100% parity achieved on all shared JSON vectors.
- Python `pytest` and JS `vitest` both pass the same vectors.
- JS SDK successfully builds with `tsup`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-sdk/03-05-SUMMARY.md`
</output>
